# OpenSpec — 开放需求规格

## 目录结构

```
openspec/
  specs/       # 需求规格文件（待实现）
  changes/     # 已实现的变更记录
    archive/   # 历史归档
```

## 规范

- `specs/` 下的 `.md` 文件描述待实现的功能需求
- 需求实现后，从 `specs/` 移至 `changes/`，按时间或版本归档
- 每个文件包含：需求描述、改动范围、不涉及范围
